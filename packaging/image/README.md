# Image provisioning

Repeatable Pi provisioning scripts. Not implemented yet.

**No secrets here, ever.** This repository is public. Provisioning reads from an
untracked `config.local.json`; the tracked template is `config.example.json` at the
repo root.
